PORT=3001
CDP_NETWORK=base
ENTRY_FEE_USDC=0.001
SANDBOX_MODE=true
ENABLE_ONCHAIN_PAYOUTS=true
PAYOUT_INTERVAL_MS=86400000

# Recipient and contracts (use test values locally)
CDP_RECIPIENT_ADDRESS=
PRIZE_POOL_CONTRACT=
PRIVATE_KEY=

# RPC / WS endpoints (optional for local test)
BASE_RPC_URL=
# BASE_WS_URL=

# Security
ADMIN_TOKEN=local-dev-token
CORS_ORIGINS=http://localhost:4174

# Facilitator configuration
FACILITATOR_URL=https://x402.org/facilitator

# Coinbase API keys (only for mainnet; leave unset on testnet)
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=